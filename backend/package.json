{
  "name": "gis-backend",
  "version": "1.0.0",
  "description": "Express backend for GIS locations app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand",
    "db:seed": "node scripts/seedFromJson.js",
    "db:seed:truncate": "node scripts/seedFromJson.js --truncate"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.1.1"
  }
}
